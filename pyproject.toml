# Metadata only; the harness runs in place via pythonpath, it is not
# installed as a package. Dependencies stay pinned in requirements.txt.
[project]
name = "prompt-eval-harness"
version = "1.0.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
